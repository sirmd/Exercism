{
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "program": "${workspaceFolder}\\javascript\\two-bucket\\two-bucket.js",
            "cwd": "${workspaceFolder}\\javascript\\two-bucket"
        }
    ]
}